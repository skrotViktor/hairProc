# Hair procedural for Hydra 2.0

Deform source BasisCurves to a target geometry using OpenCL in Hydra 2.0

## Build

Build with cmake. Currently, the CMAKE_INSTALL_PREFIX, USD_INSTALL_ROOT and HOUDINI_INSTALL_ROOT are set inside the root CMakeLists.txt file.
Tested only on Mac.
